const sessionExpireTime = 5 * 60 * 1000;

const sessionCookieName = 'ASP.NET_SessionId';
const csrf1CookieName = '__RequestVerificationToken';
const csrf2CookieName = '__RequestVerificationToken2';

export {
  sessionCookieName,
  sessionExpireTime,
  csrf1CookieName, 
  csrf2CookieName
}
